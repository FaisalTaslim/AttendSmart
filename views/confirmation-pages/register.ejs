<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Registration Successful</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <style>
        :root {
            --accent: #DE3163;
            --accent-hover: #C62856;
            --accent-rgb: 222, 49, 99;

            --bg-main: #0f0f14;
            --card-bg: rgba(255, 255, 255, 0.08);
            --border-glow: rgba(var(--accent-rgb), 0.35);

            --text-primary: #ffffff;
            --text-secondary: #e2dbe2;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
        }

        body {
            min-height: 100vh;
            background:
                radial-gradient(
                    circle at top,
                    rgba(var(--accent-rgb), 0.25),
                    transparent 60%
                ),
                linear-gradient(180deg, #0c0c11, #141420);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--text-primary);
        }

        .success-card {
            width: 100%;
            max-width: 520px;
            padding: 2.8rem 2.5rem;
            background: var(--card-bg);
            backdrop-filter: blur(18px);
            border-radius: 20px;
            border: 1px solid var(--border-glow);
            box-shadow:
                0 0 40px rgba(var(--accent-rgb), 0.25),
                inset 0 0 0 1px rgba(255, 255, 255, 0.05);
            text-align: center;
        }

        .icon {
            width: 78px;
            height: 78px;
            margin: 0 auto 1.4rem;
            border-radius: 50%;
            background: rgba(var(--accent-rgb), 0.18);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2.2rem;
            color: var(--accent);
            box-shadow: 0 0 18px rgba(var(--accent-rgb), 0.6);
        }

        h1 {
            font-size: 1.8rem;
            font-weight: 600;
            margin-bottom: 0.8rem;
        }

        p {
            font-size: 1rem;
            line-height: 1.6;
            color: var(--text-secondary);
            margin-bottom: 2rem;
        }

        .action-btn {
            display: inline-block;
            padding: 0.75rem 1.8rem;
            border-radius: 999px;
            background: var(--accent);
            color: #fff;
            font-size: 0.95rem;
            font-weight: 500;
            text-decoration: none;
            box-shadow:
                0 10px 30px rgba(var(--accent-rgb), 0.45);
            transition: all 0.25s ease;
        }

        .action-btn:hover {
            background: var(--accent-hover);
            box-shadow:
                0 12px 36px rgba(var(--accent-rgb), 0.65);
            transform: translateY(-1px);
        }

        .footer-note {
            margin-top: 1.6rem;
            font-size: 0.8rem;
            opacity: 0.7;
        }
    </style>
</head>

<body>

    <div class="success-card">
        <div class="icon">✓</div>

        <h1>Registration Successful</h1>

        <p>
            <b>DO NOT REFRESH THE PAGE</b> <br>Your account has been verified successfully.<br>
            Check your registered email for login credentials and
            complete the setup from your dashboard.
        </p>

        <a href="/login" class="action-btn">
            Go to Login
        </a>

        <div class="footer-note">
            AttendSmart · Secure Registration
        </div>
    </div>

    <script>
        setTimeout(() => {
            window.location.replace("/");
        }, 10000);
    </script>

</body>
</html>
