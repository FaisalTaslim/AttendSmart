<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employee QR</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #f4f4f4;
        }

        .display-qr {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 50vmin;
            aspect-ratio: 1 / 1;
        }

        .display-qr>img {
            width: 100%;
            height: 100%;
            aspect-ratio: 1 / 1;
            object-fit: contain;
            box-shadow: 0 0 25px rgba(0, 0, 0, 0.25);
            border-radius: 12px;
            background: white;
        }
    </style>
</head>

<body>
    <div class="display-qr">
        <img src="<%= qrImage %>" alt="Failed to load">
    </div>
</body>

</html>